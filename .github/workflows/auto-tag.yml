name: Auto Tag

on:
  push:
    branches:
      - main
    paths:
      - "crates/ccs/Cargo.toml"

jobs:
  auto-tag:
    uses: tensakulabs/.github/.github/workflows/auto-tag.yml@main
    with:
      version_file: crates/ccs/Cargo.toml
    permissions:
      contents: write
